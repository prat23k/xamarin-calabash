dependencies:
  override:
    - gem install calabash-android

test:
#  pre:
#    - circle-android wait-for-boot
#    - sleep 5
#    - adb shell input keyevent 82
#    - sleep 5
#    - adb shell input touchscreen swipe 370 735 371 735
#    - sleep 5
#    - adb shell input touchscreen swipe 370 735 371 735
#    - sleep 5
  override:
             - bundle install
             - gem install xamarin-test-cloud
#             - calabash-android resign "`pwd`/prebuilt/atlas.apk"
             - bundle exec calabash-android build /home/ubuntu/xamarin-calabash/prebuilt/atlas.apk
             - test-cloud submit "`pwd`/prebuilt/atlas.apk" a9c971049636ba5debb3b8eda661721e --devices c8bd65b8 --series "master" --locale "en_GB" --user prathima.kalluri@sky.uk --profile "android" --config "`pwd`/config/cucumber.yml"