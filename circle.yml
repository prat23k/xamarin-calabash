dependencies:
  override:
    - gem install calabash-android

test:
#  pre:
#    - circle-android wait-for-boot
#    - sleep 5
#    - adb shell input keyevent 82
#    - sleep 5
#    - adb shell input touchscreen swipe 370 735 371 735
#    - sleep 5
#    - adb shell input touchscreen swipe 370 735 371 735
#    - sleep 5
  override:
             - bundle install
             - gem install xamarin-test-cloud
#             - calabash-android resign "`pwd`/prebuilt/atlas.apk"
             - bundle exec calabash-android build /home/ubuntu/xamarin-calabash/prebuilt/xxx.apk
             - test-cloud submit "`pwd`/prebuilt/atlas.apk" xxx --devices xxx --series "master" --locale "en_GB" --user xx@mail.com --profile "android" --config "`pwd`/config/cucumber.yml"